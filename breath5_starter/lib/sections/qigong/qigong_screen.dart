import 'package:flutter/cupertino.dart';

class QigongScreen extends StatelessWidget {
  const QigongScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Qigong Section'),
    );
  }
} 