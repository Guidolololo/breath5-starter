import 'package:flutter/cupertino.dart';

class TummoScreen extends StatelessWidget {
  const TummoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tummo Section'),
    );
  }
} 