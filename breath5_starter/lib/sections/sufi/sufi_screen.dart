import 'package:flutter/cupertino.dart';

class SufiScreen extends StatelessWidget {
  const SufiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sufi Section'),
    );
  }
} 